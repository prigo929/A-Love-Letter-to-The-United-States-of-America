# Project instructions

- After editing `.ts`/`.tsx` files, run `cd web && npx tsc --noEmit` to type-check. **Do not run `npm run build`** — it is slow and the user has asked it not be run as part of routine verification. Only run a full build if the user explicitly asks for one in that turn.
